# 한백 EV 콘솔 — 작업 규칙

## 큰 그림: 관리 도구를 노션에서 콘솔로 일원화한다

1. ~~병행기~~ → **이관 완료 (2026-08-24)** — 노션 「26 전기차 계약/현장관리」 140건이 콘솔
   프로덕션 DB 에 들어갔다(`scripts/import-notion-2026.ts`, 스냅샷·백업은
   `~/hanbaek-backups/20260823/`). 시험 현장은 백업 후 삭제했다.
2. **컷오버 완료 (2026-08-26)** — ★포털 접수를 닫았다★. 접수는 콘솔에서만 받는다
   (`/projects/new` — 로그인한 소속으로 들어와 콘솔 DB 에 바로 남는다). 무로그인 접수의
   소속(actor) 매핑을 설계하는 대신 **문을 닫는 쪽**을 골랐다(한백 결정) — 협력사는 업체마다
   계정 하나로 들어온다. 닫은 자리 넷: 포털 접수 화면(안내로 바뀜) · `POST /api/intake` ·
   `/api/upload` 의 접수 ZIP 토큰 · 포털 입구 링크. 이유와 갈 곳은 `lib/portal-intake.ts` 한 곳이다.
   그 뒤로 노션에 새로 쌓이는 접수는 없다 — 손 이관도 끝났다.
3. **그 뒤** — 관리는 콘솔에서만 한다. 노션은 걷어낸다.
   **포털은 협력사의 로그인 없는 입구(계약서 작성 + 접수)로 계속 남는다.**

**dual-write 금지**는 그대로다 — 한 필드의 정본은 한 곳. 26년 현장의 정본은 이제 콘솔 DB 다.
정산관리도 이관했다 — ★2026-08-28 로 끝났다★. 지급 원장 110건(4억 6,889만)이 콘솔에 있고,
노션 「26 정산관리」는 제목을 「(닫힘 · 콘솔이 정본)」으로 바꾸고 설명에 안내를 넣어 닫았다.
그 시트는 이력 보관용이다 — 거기에 지급을 새로 적으면 dual-write 다. 2차 지급도, 기성 수금도
이제 콘솔에서 기록한다.

## 지금 하는 일: 콘솔 픽스

**최우선은 흐름이다 — 접수부터 정산까지 한 현장이 끊기지 않고 흘러가는 UX.** 화면 하나가
아니라 화면 사이가 매끄러워야 하고, 조작이 가능한 자리는 실제로 눌러볼 수 있어야 한다 —
보이기만 하는 화면은 절반이다.

### 다음 할 일 (위에서부터 — "계속해" = 맨 위를 진행한다)

00. **감사 나머지 — 중간 34건 · 낮음 29건** (2026-09-04 시작, 미완)

    전 화면 로직 감사를 돌렸다(화면 묶음 12곳 병렬 검토 → 발견 86건 → 반박 검증 →
    확정 72건). ★높음 9건은 그날 다 고쳐 배포했다★ — 준공서류 없는 준공완료(2차 지급이
    열렸다) · 설치완료 선언 서버 미검증(시공비 1차) · 계약중단 현장 지급 · blobUrl 임의
    삭제 셋 · 부담주체 협력사 노출 · 설치승낙서 업로드 불가. **남은 것이 중간 14 · 낮음 21 이다(2026-09-09 기준 — 중간 20·낮음 8 을 고쳤다).**

    목록은 보고서에 있다(칸마다 「고침 표시」 체크가 달려 있다):
    https://claude.ai/code/artifact/de5ee791-47e4-4411-8e74-4e4dfc8f616d

    ~~M10 같은 칸 동시 업로드로 파일이 조용히 사라지는 경합~~ 고쳤다(2026-09-08, `uploadDocument` 의 칸 단위
    자문 잠금 — `test/db/upload-race.test.ts` 가 재현·회귀 그물). M9(착공 뒤 반려 교착)는 8a35d50 으로 이미 고쳐졌다.
    ~~M18·M19·M22·M23·M31·L10 지급·정산 원장의 문 여섯~~ 고쳤다(2026-09-09) — 확정 배치에 손으로 적기 ·
    같은 회차 동시 확정 · 계산서 둘 붙은 배치 합치기 · 계산서 붙은 배치의 마지막 줄 빼기 · 수금 뒤 규칙 변경 ·
    계약 확인과 반려의 경합. 전부 저장소에 문(잠금·검사)을 넣었고 `test/db/payout-integrity.test.ts` 가
    재현·회귀 그물이다(픽스처 `test/db/money-kit.ts` — 영업비 1차가 열린 현장을 만들고 배치·계산서까지 걷는다).
    ~~L6·M11·L20·M24·M29·M30~~ 고쳤다(2026-09-09) — 반려 해제 뒤 담당 복귀 · 보완요청 취소가 진짜 반려까지
    지우던 것(`documents.asked_at`, migrations/0068 — 요청이 세운 반려만 되돌린다; 요청은 빈 칸에만) · 대행 중 공지
    읽음이 협력사에 찍히던 것 · 표준 파일명의 분류 넷 · 열람 전용의 기성 탭 · 수금률의 실수금액. 시험:
    `test/db/review-court.test.ts` · `notices-read.test.ts` · `test/files-standard-name.test.ts` · `collection-rate.test.ts`.
    ~~M20·L12·L9~~ 고쳤다(2026-09-09) — 지급일 후보가 이미 지난 날짜로 잡히던 것(`payDateChoices` 가 앞으로 올
    10·25일 둘을 준다) · 공정 서류의 반려 해소(재업로드)가 이력에 안 남던 것 · 임시본 유실 재시도가 「칸에 아무 파일」로
    성공하던 것(그 파일 이름으로 본다). ~~M5·M16·M17~~ 은 다른 세션이 이미 고쳤다(01590bf · b529fd5).
    ~~M7·L14·L11~~ 고쳤다(2026-09-09) — 환경부 대기번호 칸이 남의 수정을 옛 DOM 값으로 덮던 것(key 로 다시 그림) ·
    단가 케이스 동시 추가가 같은 칸 두 벌을 만들던 것(잠금 안에서 중복 재판정, `test/db/pricing-rule-race.test.ts`) ·
    보완요청이 조사반려 사유를 덮던 것(요청은 빈 칸에만 — M11 과 같은 고침). 접수 일괄 라우트의 재시도 판정(L9 나머지)도 마쳤다.
    ~~M25·M26·M33·M34~~ 고쳤다(2026-09-09) — 임시 자리로 올리는 중에도 눌리던 접수 단추 · 「전체 받기」 ZIP 안에서
    같은 이름이 서로를 덮던 것(`uniqueFileName`) · 갈 곳 없는 계약서 작성 링크의 ?org · 주소를 손으로 고쳐 재조회할 때
    지난 검색의 지번이 남아 다른 현장 이력이 「매칭」으로 나오던 것.
    ~~L21 /finance·/scan 이 CONSOLE_PATHS 에 빠져 미들웨어 두 문 우회~~ 고쳤다(2026-09-09, `lib/routes-map.ts` +
    `test/conventions/console-paths.test.ts` 가 재발을 막는다).
    중간 중 눈에 띄는 것: 착공 뒤 계약서류를 반려하면 협력사가 못 고치는 교착 ·
    같은 칸 동시 업로드로 파일이 조용히 사라지는 경합(잠금 없는 read-modify-write) ·
    legacylog 를 올리면 조사 결과가 기본값 '없음' 으로 확정 표시 · 열람 전용이 기성 탭을
    못 봄(눈 자리에 손으로 잠갔다) · 수금률이 실수금액을 무시하고 계획액으로 셈.

    ★남은 하나 더 — H3 회수★: 계약중단(계약파기)한 인천 남동구 에코메트로 타워 A동에
    2026-07-10 엘앤에스로 나간 영업비 1차 280만원이 회수 대상이다(한백 확인). 「다른 현장
    잔금에서 차감」으로 방향은 정했으나 ★어느 현장에서 뺄지 미정★ — 그날 「일단 냅둬」로
    멈췄다. 반달마을푸르지오 초과 지급 28.5만도 같은 엘앤에스 건이라 함께 정리하면 된다
    (아래 5번 항목의 남은 일). 잔액 보는 자리: `scripts/check-org-balance.ts`.

00-1. **하네스 — 바이브코딩에서 관문으로** (2026-09-08 시작). 규모가 커져 사람 눈 대신 기계가 잡아야
    한다. 그물의 재료는 있었다(테스트 429개 0.5초 · tsc strict · lint · 마이그레이션 가드 · check:leak ·
    perf). 없던 것은 사람이 안 돌려도 도는 자리다 — 백업이 2주 조용히 실패한 것이 증거.
    ~~1 세션마다 워크트리·브랜치, main 은 머지로만, 브랜치마다 프리뷰~~ 했다(2026-09-08, 「협업 방식」).
    ~~2 CI 관문~~ 했다(2026-09-08) — `.github/workflows/ci.yml` 이 브랜치 푸시마다 tsc·test·lint·마이그레이션
    번호 검사를 돌린다(약 1분). main 보호 규칙(검사 `check` 통과 필수 · 관리자 포함 · 강제 푸시·삭제 금지 ·
    선형 이력)은 2026-09-09 켰다 — 통과 전의 `push HEAD:main` 은 원격이 거부한다.
    ~~3 경계 테스트~~ 했다(2026-09-08) — `test/db/`(13개 · 개발 DB · 약 9초) · `npm run test:db`. 라우트 응답
    계약·저장소의 문·협력사 가시성·동시 업로드를 본다. M10(같은 칸 동시 업로드 유실)을 시험이 먼저 잡고
    자문 잠금으로 고쳤다. 남은 감사 항목은 이 방식으로 — 재현 시험 → 고침 → 초록.
    ~~4 실패가 소리 나게~~ 했다(2026-09-09) — 감시 `~/hanbaek-backups/watch-prod.sh`(launchd 10분)가 배포·CI·
    백업·런타임 넷을 보고 상태가 바뀔 때 macOS 알림 한 줄(「운영」 절). 주간 백업 스크립트도 실패·경고·성공을
    알린다. 라우트의 예상 밖 오류는 500 + `[api]` 로그로 바꿔 감시가 볼 수 있게 했다(코드 규칙).
    ~~5 이 문서의 「지켜라」를 lint 규칙·테스트로~~ 했다(2026-09-09) — lint 둘(Promise.all 안의 쿼리 체인 ·
    태그 없는 console.error), 관례 시험 넷(`test/conventions/` — 쓰기 라우트의 문 · /admin 화면의 (write) ·
    콘솔 경로 목록과 화면 폴더 1:1 · 설정 불변식), CI 한 단계(기존 마이그레이션 수정 거부). 경로 목록은
    `lib/routes-map.ts` 로 모았다. 문장은 「왜」로 남고 「무엇」은 기계가 본다.
    **하네스 다섯 단계가 끝났다.** main 보호도 2026-09-09 켰다(검사 `check` 필수 · 관리자 포함 · 선형 이력).
    남은 것은 도구가 아니라 습관이다: 남은 감사 항목(00)을 「재현 시험 → 고침 → 초록」으로 하나씩.
    ★발견★ 프로덕션 DB 주간 백업(launchd, 월 09:30)이 08-31·09-07 「DIRECT_URL 없음」으로 실패했다 —
    launchd 는 `~/Documents` 를 못 읽는다(macOS 폴더 보호). 접속 파일의 진짜를 `~/hanbaek-backups/.env.prod-db`
    로 옮기고 저장소 `.env.prod-db` 는 그리로 가는 링크로 두었다. 09-14 스케줄에서 성공을 확인한다.

0. **리팩토링** — 정본은 `doc/REFACTOR_PLAN_3.md`(클린코드 층까지). 2 번 문서의 항목은
   거기서 층에 맞춰 다시 배치했다. ~~B 무로그인 상한~~(=T0, 2026-08-27) · ~~F 단위 테스트~~
   (2026-08-28, 146개) · ~~G 배치 헬퍼~~(`lib/payout-board.ts`) 완료.
   ~~0층 안전망~~·~~1층 위생~~(2026-08-28) · ~~2층 경계~~ 완료(2026-08-29):
   저장소 2,844 → 581줄로 분해(`lib/data/store/` 일곱) · `lib/data` 아래 80줄 넘는 함수 0개 ·
   화면 셋(단가 1,779 → 208 · 시공 탭 1,266 → 570 · 접수 폼 1,077 → 609) 분해.
   **다음: 3층 · 중복의 뿌리** — 3-1 운영사 어댑터(운영사 하나 붙일 때 다섯 곳을 고치는
   중복)부터. 화면 파일 넷이 아직 400줄을 넘는데, 줄 수가 아니라 **상태 묶기**의 문제라
   3층에서 같이 본다(계획서 2-3 참고).
1. ~~**흐름 워크스루**~~ 했다 (2026-08-29) — 현장 하나를 접수부터 기성 수금까지 콘솔로
   끝까지 밀었다(개발 DB, 한백·턴키 두 역할). 끊김 일곱을 고쳤다(커밋 022a329): ★새 현장이
   준공을 못 끝내던 것★(준공서류 완료 조건이 화면에 없는 옛 칸을 봤다) · 「필수 서류 0건
   남음 — 접수 불가」 · 「… 고르세요 필요」 · 시공 배지 0/14 고정 · 끝난 현장이 할 일에 잔류 ·
   단가 후보에 지난 반기 섞임 · 「턴키」가 한 화면에서 두 값.
   ~~② 「운영사 계약서 제출」 칸이 아무의 할 일에도 안 뜬다~~ 정리됐다 (2026-08-29) —
   담당은 운영사가 맞다(우리가 낸 뒤 운영사가 환경부에 접수하고, 승인이 나면 운영사가
   승인일과 함께 알려준다). ★할 일에는 세우지 않는다★ (한백 지시) — 기다리는 것은 할 일이
   아니다. 대신 보드에서 보인다: 시공 첫 칸 「환경부 승인 대기」에 카드가 서고 정체일이 붙는다.
   ~~① 시공 탭 첫 구간이 「오지 않은 구간」~~ 고쳤다 (2026-08-29, 한백 지시) — 그 칸을
   시공 보드·스테퍼의 첫 자리에 「환경부 승인 대기」로 세웠다. 같은 자리를 계약은 「냈다」로,
   시공은 「기다린다」로 부른다(`lib/board.ts` HANDOFF_STATUS).
   **남긴 것** — 고칠지 한백이 정한다: ③ 지급 가능한 현장은 할 일에 없다(지급관리 화면에서
   체크해 가확정하는 방식이라, 지금이 맞을 수도 있다).
2. 준공보완 때 「무엇을 보완하라」를 전하는 길 — **한백이 다음 주에 정해준다.**
   공정 서류에는 반려·사유가 없어서(제출=통과) 지금은 진행현황 메모뿐이다.
3. ~~기성 수금 체크·준공마감 지정~~ 완료(2026-08-28) — 현장 상세의 기성 탭에서 준공마감일을
   지정하고 차수별로 수금을 기록한다. ★수금은 날짜와 「실수금액」을 같이 받는다★ —
   협의로 턴키단가와 다르게 받는 현장이 있어서다(익산 예다음아르띠에: 케이스 150만/기,
   실제 190만/기). 금액을 비우면 계획액대로 받은 것이고, 적으면 그 금액이 수금액이 된다
   (`migrations/0034`). 열리지 않은 차수에는 수금을 못 적는다 — 저장소가 막는다.
4. ~~개발 DB 분리~~ 완료(2026-08-22) · ~~Blob 분리~~ 완료(2026-08-23) · ~~26년 현장 이관~~
   완료(2026-08-24).
5. **이관 후속** — ~~① 정산관리 이관~~ 완료(2026-08-28, `scripts/import-notion-settlements.ts`) —
   지급 원장 110건 4억 6,889만. 노션 시트는 닫았다. **남은 일 둘**: 기성 수금은 노션에도
   기록이 없어 옮길 것이 없었다(넣을 자리는 만들었다 — 기성 탭) · 반달마을푸르지오 초과
   지급 28.5만을 시공비 잔금에서 차감. ~~② 에버온 환경부 5년 **한전불입** 단가
   케이스~~ 완료(2026-08-26, `migrations/0025`) — 0006 이 「정책표 한전인입지역 5년 칸이
   비었으니 케이스가 없다」로 읽고 지운 것을 되살렸다. 빈칸은 케이스 없음이 아니었다:
   인입지역 7·10년 금액이 일반 환경부 행과 같아 금액 가산이 아니고(할인·회선 지원만 갈린다),
   노션 정본 매트릭스도 5년을 「모자분리/한전불입」 한 칸(220만)으로 운영했다.
   ★대우마리나 라인은 아직 미지정이다★ — 이관 때 null 로 들어갔으니 현장 상세에서 단가를
   지정한다(되살린 케이스가 유일 후보다).
   ③ 신설·교체 유형 검증(한백이 직접) ④ 나머지 협력사 계정 7곳(`/admin/accounts` 에서 —
   대광이브이·차저스랩·네이비인프라·푸름·채움에스앤시·유니온솔루션·아현테크)
   ~~⑤ 컷오버(포털 접수 → 콘솔 DB) 설계~~ — 포털 접수를 닫는 것으로 끝냈다(2026-08-26).
6. **접수 서류를 열어보지 않고 믿을 수 있게** — ★목표는 검수 기능이 아니라 「한백이 파일을
   하나하나 열어 확인하지 않아도 되는 것」이다★ (한백 2026-08-31). 지금은 아무것도 확인하지
   않는다: 분류는 ①파일명 규칙(결정적) → ②AI 판독 → ③기타 순으로 칸을 정하고, 그 판정이
   맞는지 되묻는 단계가 없다. `lib/intake-review.ts`(「「계약서」라고 분류된 문서가 실제로 그
   서류인가」를 묻는 두 번째 판독)와 화면 자리(`components/intake/DocSection.tsx` 의
   `review.findings`)는 만들어져 있는데 ★아무 데서도 안 불린다★ — 접수가 1~2분 걸리고 오탐이
   있어 껐다(`lib/intake-auto.ts` 의 `review = null` 주석).
   **손댈 자리 셋** — ⓐ 판독이 읽은 문서 제목(`ClassifiedFileInfo.title`, 2026-08-31 추가)을
   서류 칸마다 보여준다. 지금은 「기타」의 파일명에만 쓴다. 칸 이름과 제목이 어긋나면 열지
   않고도 보인다 — 가장 싸고 결정적인 방법이다(칸에 붙여 두려면 `documents` 에 자리가 필요).
   ⓑ 검수를 접수가 아니라 현장 상세에서 한백이 눌러 돌린다(껐던 이유가 접수 속도였다).
   ⓒ 같은 칸 충돌 경고를 접수 뒤에도 남긴다(지금은 접수 화면에서만 뜨고 사라진다).
   **시점** — 실제 계약접수를 한 건 받아 내부에서 돌려본 뒤에 정한다(한백).

### 개발 DB 와 파일(Blob) 모두 프로덕션과 분리됐다

**DB (2026-08-22 분리 완료)**

`.env.local` 의 DATABASE_URL·DIRECT_URL 은 개발용 Supabase 프로젝트를 가리킨다. 프로덕션
(Vercel 환경변수)은 딴 DB 다. 그래서 로컬에서 `db:push`·`db:seed --reset` 을 돌려도
프로덕션은 안 다친다 — 예전에는 같은 DB 였고 그것이 사고 직전이었다.

- 로컬 개발 DB 에는 시드 현장 5건 + 개발 계정 5개(`admin`/`ecoelec`/`daesang`/`navy`/`viewer`,
  비밀번호 전부 `dev1234!`)가 있다. `viewer` 가 열람 전용이다 — 소속 없이 전 현장을 본다.
  되돌리려면 `npm run db:seed -- --reset --with-dev-users`.
- ★**스키마·데이터 변경은 마이그레이션 파일로 한다**★ (2026-08-23 도입) — `migrations/*.sql`
  에 **멱등하게**(IF NOT EXISTS 등) 적으면 `npm run build` 가 먼저 적용한다. Vercel 프로덕션
  빌드도 같은 러너(`scripts/migrate.ts`)를 돌므로 **배포가 코드보다 먼저 DB 를 맞춘다** —
  「컬럼 없이 코드가 먼저 나가 500」(2026-08-22 실사고)이 구조적으로 안 생긴다.
  적용된 파일은 고치지 않는다(원장 `db_migrations` 에 있으면 다시 안 돈다) — 새 번호로.
  ★CI 가 잡는다★(2026-09-09) — main 과의 공통 조상 이후 `migrations/` 의 기존 파일이 고쳐지거나 지워졌으면 `check` 가 빨갛다.
  ★번호가 겹치면 러너가 DB 에 붙기 전에 빌드를 거부한다★ (2026-08-24 가드) — 동시 세션
  둘이 같은 번호를 집은 실사고(0011 두 개)의 재발 방지다. 새 파일 번호는 원장이 아니라
  **디렉터리의 최대 번호 + 1** 로 잡는다.
  로컬 개발 DB 에는 `npm run db:migrate`. `db:push` 는 마이그레이션 없는 실험용으로만 —
  push 로 만든 스키마는 프로덕션에 못 간다.
- ★**어느 Supabase 프로젝트가 프로덕션인가**★ 헷갈리면 고친 것이 안 고쳐진다.
  - **프로덕션 = `fsngrxdmucwlqnduzrhw`** — 처음 만든 프로젝트. Vercel 의 `DATABASE_URL` 이 여기다.
  - **개발·시험 = `impavoeuvywtdkeweyqd`** — 나중에 만든 것. `.env.local` 이 여기다.

  로컬에서 붙는 DB 도, Supabase SQL Editor 를 무심코 여는 프로젝트도 뒤쪽이다. 그래서
  프로덕션 스키마를 고친 줄 알았는데 개발 DB 를 고치고 마는 일이 실제로 있었다
  (2026-08-22 `pricing_rules` 정책 6칸 — SQL 은 「Success」였고 프로덕션은 그대로였다).
  **반영 전에 프로젝트 ref 를 먼저 본다** — 대시보드 주소가 `…/project/<ref>` 다.
  어디 붙었는지 가리는 법: `users` 에 `dev1234!` 개발 계정 5개(`navy`·`viewer` 포함)가
  있으면 개발 DB 다 — 프로덕션 계정은 다른 구성이다. (옛 표지였던 pricing_rules 행수는
  복원 시험 뒤 양쪽이 같아져 못 쓴다, 2026-08-24)
- ★**프로덕션 DATABASE_URL 은 Vercel 에서 Sensitive 다 — 값을 되읽을 수 없다.**★
  `vercel env pull`·`vercel env run` 으로도 안 나온다(`env run` 은 `.env.local` 을 덧씌운다).
  **로컬에서 붙는 길은 `.env.prod-db`(비추적, `DIRECT_URL` 하나) 뿐이다** — 스크립트는
  `--env .env.prod-db` 로 받는다(`scripts/apply-nice-h2-pricing.ts` 의 방식). ★진짜 파일은 `~/hanbaek-backups/.env.prod-db` 고 저장소의 것은 링크다★(2026-09-08 — launchd 백업이 `~/Documents` 를 못 읽어서). 워크트리에는 복사되지 않으니 프로덕션 DB 스크립트는 본 저장소에서 돌린다. 값이 낡으면
  (Supabase 비밀번호 rotate — 2026-08-23 실제로 겪음) Supabase 대시보드의 프로덕션
  프로젝트에서 Session pooler 접속 문자열을 다시 받아 그 파일만 고친다.
  일상 스키마·데이터 반영은 그래도 **마이그레이션**이 기본이다 — 접속 문자열이 있는
  곳(빌드)이 대신 돌아 주므로, 사람이 SQL Editor 에 붙여넣던 절차와
  「어느 프로젝트에 돌렸나」 사고(위 ref 항목)가 같이 사라진다. `.env.prod-db` 는
  이관·백업처럼 마이그레이션으로 못 하는 일에만 쓴다.
  예외 하나: `login_attempts` 는 `lib/auth/throttle.ts` 가 처음 쓸 때 스스로 만든다.
  로그인을 막지 않는 부속물이라서 둔 예외고, 다른 표에 이 방식을 늘리지 않는다.
- **프로덕션이 왜 그러는지는 런타임 로그로 본다** — `npx vercel logs --environment production
  -x --since 30m`. 화면에 안 나오는 조용한 실패(위 표 없음이 그랬다)는 여기 `[auth]`·`[db]`
  줄이 유일한 신호다. `npx vercel ls` 로 배포가 실제로 Ready 인지도 먼저 확인한다.
  ★2026-09-09 부터 사람이 열지 않아도 감시가 본다★ — `~/hanbaek-backups/watch-prod.sh`(launchd
  `com.hanbaek.prod-watch`, 10분마다)가 최근 production 배포 상태 · CI 마지막 실행 · 백업 덤프 나이 · 최근 15분
  런타임 로그(error·5xx·대괄호 태그)를 보고, 상태가 바뀔 때 macOS 알림 한 줄을 띄운다(계속 나쁘면 하루 한 번).
  지금 상태는 `zsh ~/hanbaek-backups/watch-prod.sh --print` — 세션을 시작할 때 한 번 보면 프로덕션이 깨진 채
  일하는 것을 막는다. 알림 판단은 `watch-notify.py`, 상태·로그는 `~/hanbaek-backups/auto/watch-*`.
  저장소 밖에 두는 이유: launchd 는 `~/Documents` 를 못 읽는다(주간 백업 `backup-prod.sh` 가 그래서 2주 실패했다).
  한계: 이 Mac 이 켜져 있고 로그인돼 있을 때만 — Slack·메일로 보내려면 `watch-notify.py` 의 notify 에 웹훅 한 줄.
- 프로덕션 DB 를 봐야 할 때만 접속 문자열을 바꿔 쓰고, 끝나면 개발 DB 로 되돌린다.
- 프로덕션의 첫 관리자는 `npm run auth:bootstrap`, 다음 사람부터는 `/admin/accounts`.

**파일(Vercel Blob) — 분리 완료 (2026-08-23)**

로컬(`.env.local`)의 `BLOB_READ_WRITE_TOKEN` 은 개발용 빈 스토어(`hanbaek-dev2`,
development 환경에만 연결)를 가리키고, 프로덕션·프리뷰는 원래 스토어
(`hanbaek-uploads-2`)를 그대로 쓴다. 로컬에서 올리고 지워도 실파일(계약서·사업자등록증·
통장사본)은 못 건드린다. 부작용 하나: **로컬 자료실(/library·/admin/materials)은 빈
목록이다** — 자료실은 DB 없이 스토어를 직접 나열해서 그렇다. 프로덕션 자료실은 무관하다.

`npm run dev` → http://localhost:3000/projects

---

## 사업 구조

- **포털** (`app/(portal)/`, 로그인 없음, `hanbaek-form.vercel.app`) — 협력사가 계약서를 쓴다
  (접수는 2026-08-26 에 닫았다). **동작은 바꾸지 않는다 — 운영 중이다.** 보이는 것은
  정리했다(2026-08-29 한백 지시): 업데이트 안내를 걷어내고, 영문 아이브로우(CONTRACT ·
  LOOKUP · RESOURCE LIBRARY · K-APT LOOKUP)와 설명 문구를 지우고, 한 기둥으로 세웠다.
  ★콘솔의 화면 규칙을 포털에도 적용한다★ — 두 앱이 다른 말투를 쓰면 같은 사람이 두 번 배운다.
- **콘솔** (`app/(console)/`, 로그인, `hanbaek-ev.vercel.app`) — 한백이 현장을 진행시킨다. 지금 만드는 것.

주소가 둘이고 배포는 하나다(2026-08-22). 포털 주소에서 콘솔 경로는 404 이고, 콘솔 주소는
포털 화면까지 다 열린다 — 콘솔의 「계약서 작성」이 포털 양식을 쓰기 때문이다. 가르는 자리는
`middleware.ts` 하나다. `.vercel.app` 은 Public Suffix List 라 두 주소가 쿠키를 못 나눈다.

**내는 사람은 언제나 협력사다 — 한백은 접수하지 않는다.** 길은 둘이다: 포털 `/intake`
(로그인 없음 → 노션, 실무가 도는 길)와 콘솔 `/projects/new`(로그인한 협력사 → 콘솔 DB,
세션의 소속으로 현장이 만들어져 보드의 「계약접수」 칸에 선다).

## 돈의 흐름

지급 트리거는 영업비 1차=계약서류 확인 완료(contractConfirmedAt), 시공비 1차=설치완료,
2차는 둘 다 준공완료다(2026-08-31 개통완료에서 옮김 · 이름은 2026-09-02 한백 지시).

★트리거 이름 「설치완료」는 칸 이름이 아니다★ — 그 칸은 2026-09-04 에 「개통 및 통신확인」
으로 갈렸다(한백 지시 · migrations/0052). 시공비 1차를 여는 것은 「설치가 끝났다」는
사실(installConfirmedAt)이고, 그것은 그 칸에 ★들어오는★ 조건이다 — 들어와서 하는 일이
통신·개통이라 칸 이름만 바꿨다. 영업비 1차를 「계약완료」 칸과 갈라 「계약서류 확인 완료」로
부른 것과 같다. 지급관리의 「1차 · 설치완료 시 70%」를 칸 이름에 맞춰 고치지 않는다.

운영사 ─ 기성(트리거가 연다: 환경부 승인·착공·준공마감) → **한백** ─ 지급(영업비·시공비,
1차 70% → 2차 잔액) → 하도급사(영업사·시공사). **한백 몫 = 받을 기성 − 내려줄 지급.**

지급 확정은 두 단계다(2026-08-24): **가확정**(지급관리 표에서 체크 → 배치. 협력사 할 일에
「세금계산서 발행」이 떠 그 합계로 발행 — 회전 1~2일) → **확정**(배치 줄에서 바로, 첨부도
줄에서. 계산서와 무관하게. 배치가 잠긴다 — 빼기·지급일 변경·취소는 해제 후에만. 명세서
상세는 검토·인쇄·예외 처리용). 이달 조건 충족분은 익월 10·25일이 기본이고 다른 날도
지정할 수 있다(가확정 바의 「다른 날」).
협력사는 눈으로만 본다 — 확정·첨부 전부 한백의 일이다. 세금계산서는 검토 없는 보관용
첨부다(명세서마다 한 장, 확정 여부와 무관하게 붙이고 바꾼다). 확정 상태의 정본은
batch_finals 테이블. 「1차 확정」이라 부르지 않는다 — 1차·2차는 회차의 말이다.
**배치 = 지급처 × 구분 × 지급일** — 영업비와 시공비는 세금계산서를 따로 끊는다(한백 확인).
같은 지급처·지급일이라도 명세서·계산서·확정이 구분별로 따로 돈다.

화면 셋이 화살표를 하나씩 본다: `/receivables` 받을 돈 · `/payouts` 줄 돈 · `/statements`
나간 돈(월별 그래프 + 배치 상태). 현장 하나의 전모는 현장 상세의 정산 탭이다.
★`/payments`(지급 내역)는 `/statements` 로 합쳤다 (2026-08-28)★ — 둘이 같은 원장을 각자
읽어 같은 줄을 「나간 지급」과 「확정 누락」 두 말로 부르고 있었다. 옛 주소는 넘겨보낸다.

## 정본 지도 — 어느 데이터의 진짜가 어디 사는가

| 데이터 | 정본 | 비고 |
|---|---|---|
| 26년 현장 | **콘솔 DB** (2026-08-24 이관, 140건) | `mgmt_no` = 노션 번호(추적용). 포털 접수를 닫아(2026-08-26) 노션에 새로 쌓이는 접수는 없다 |
| 26년 지급 원장 | **콘솔 DB** (2026-08-28 이관 완료, 110건) | 노션 「26 정산관리」는 닫았다(제목에 표시) — 이력 보관용 |
| 기성 수금 | **없음** | 노션에도 기록이 없다(회수 체크·회수일 141건 전부 빈칸). 콘솔의 「받는 돈」이 0 인 이유 |
| 콘솔의 현장 | Postgres — 프로덕션과 개발 DB 가 따로 (2026-08-22 분리) | 당분간 전부 시험 데이터 |
| 계정 | Postgres `users` | 실계정. `/admin/accounts` |
| 단가 케이스 | Postgres — `/pricing` 에서 관리 | 라인이 참조하기 전에는 수정 가능 · 참조 뒤에는 불변(개정·중지만) |
| 정산 규칙 | Postgres `settlement_rules` | 단가 케이스가 기성 단계로 정의하면 같은 모양을 찾아 재사용, 없으면 자동 생성. 이름도 단계에서 자동. 코드 시드는 초기값일 뿐 |
| 서류 종류·필수 규칙 | 코드 `lib/doc-rules.ts` | |

## 화면 지도

| 주소 | 무엇 | 누가 |
|---|---|---|
| `/dashboard` | 수주 현황 — 월별·누적, 단위는 대수 (화면 이름은 「대시보드」였다) | 전부 (자기 것만) |
| `/projects` · `/construction` | 계약관리 · 시공관리 보드 — 칸이 단계, 담당·정체일. 사이드바에 각 업무의 할 일 수가 붙는다. ★「운영사 계약서 제출」 칸은 양쪽에 선다★ — 계약의 끝이자 시공의 시작이라 시공 쪽에서는 「환경부 승인 대기」로 부른다(`lib/board.ts` HANDOFF_STATUS) | 전부 (자기 것만) |
| `/projects/[id]` | 현장 상세 — 계약·시공·협력사 지급 탭, 한백은 기성 탭까지 | 전부 (자기 것만) |
| `/projects/new` | 서류 접수 — 세션의 소속으로 낸다 | 전부 |
| `/contracts` | 계약서 작성 입구 — 포털 양식으로 소속을 실어 보낸다 | 전부 |
| `/payouts` | 협력사 지급관리 — 전 현장 지급현황, 지급 가능 줄을 체크해 가확정 | 전부 (체크·가확정은 관리자) |
| `/receivables` | 운영사 기성관리 — 차수별 상태·수금. 사이드바에 기성 할 일 수가 붙는다 | 한백(관리자·열람 전용) |
| `/statements` | 협력사 거래명세서 — 위는 월별 지급 그래프, 아래는 배치(지급처×구분×지급일) 상태·세금계산서 첨부·최종 확정. 사이드바에 지급 할 일 수가 붙는다 (2026-08-28 지급 내역을 합쳤다) | 전부 (협력사는 자기 배치만 · 확정·첨부는 관리자) |
| `/payments/statement` | 거래명세서 한 장 — 배치의 원장 줄. 인쇄물로 그대로 쓴다 | 전부 (협력사는 자기 것만) |
| `/pricing` | 단가 케이스 관리 | 한백만 |
| `/library` · `/lookup` · `/apartments` | 자료실 · 충전기/보조금 이력 · K-apt 조회 — 포털과 같은 컴포넌트 | 전부 |
| `/settings` | 사업자 정보 — 자기 사업자등록증·정산 계좌를 적는다 (도메인 이름은 그대로 「협력사 정보」) | 협력사 (한백은 `/admin/partners` 에서 전 계정을) |
| `/receivables` · `/pricing` | 위 두 줄과 같은 화면 — 열람 전용도 본다(읽기만) | 한백(관리자·열람 전용) |
| `/admin/partners` | 협력사 정보 — 계정마다 사업자등록증·계좌·통장사본 | 한백(관리자·열람 전용) — 재무팀이 지급 전에 본다 |
| `/admin/accounts` | 계정설정 — 계정 등록·구분·소속·중지 | 관리자만 |
| `/admin/materials` | 자료실 관리 — 상단 바의 자료실 옆 | 한백만 |
| `/reissue` | 서류 재발행 — 옛 양식 스캔본을 최신 양식으로. 협력사는 사이드바 접수 묶음, 한백은 상단 바 (2026-08-28 협력사에 열었다) | **전부** — 열람 전용도 (2026-08-31) |
| `/split` | PDF 분류·분할 — 스캐너가 통째로 뱉은 스캔 묶음을 서류 종류별로 갈라 받는다. 판독은 접수 ZIP 과 같은 길이다(방향 보정 → 분류 → 페이지대로 자르기), 끝만 다르다 — 현장에 붙이지 않고 받아 간다 (2026-08-29) | **전부** — 열람 전용도 (2026-08-31) |
| `/scan` | 사진 → 스캔본 — 휴대폰으로 찍은 서류를 종이만 잘라 반듯하게 펴고 그림자를 걷어 A4 PDF 로. ★서버가 하는 일이 없다★ — 전부 브라우저 캔버스다(계산은 `lib/scanify`, 시험 16개). 접수의 「휴대폰 사진으로 보임」(`lib/photo-check`)을 고칠 길이다 (2026-08-31) | **전부** — 열람 전용도 |
| `/notices` | 공지 — 한백이 협력사 전체에 알리는 글. 화면을 여는 것이 곧 「읽었다」이고, 안 읽은 수를 상단바 「공지」 배지가 단다(users.notices_read_at). 정적 안내문(public/notices/*.html)은 별개로 남아 있다 | 전부 (쓰기는 관리자만) |
| `/design` | 부품 카탈로그 — `components/ui.tsx` 를 그려 보는 화면 | 한백만 |

한백 전용은 `(admin)` 라우트 그룹의 레이아웃 한 곳이 잠근다 — 그 아래 새 화면을 만들면 저절로 잠긴다.
그 안에 문이 하나 더 있다: `(admin)/admin/(write)/layout.tsx` 는 **관리자만** 들인다(계정설정·
자료실 관리). 바깥 문은 「한백의 눈인가」까지라 열람 전용도 통과한다.

★`/admin` 아래에서 협력사 정보만 그 안쪽 문 밖에 있다★ — 재무팀(열람 전용)이 지급 전에 보는
사업자등록증·통장사본·정산 계좌다(한백 지시 2026-08-25). 괄호 그룹은 주소에 안 나오므로
`/admin/accounts` 같은 주소는 그대로다. **`/admin` 아래 새 화면은 `(write)` 안에 만든다** —
바로 밑에 만들면 열람 전용도 보게 된다. 미들웨어는 `/admin` 전체를 관리자 전용으로 두고 열어
준 주소만 뺀다(`ADMIN_READABLE`), 그래서 깜빡해도 엣지에서 한 번 걸린다.
★경로 목록은 `lib/routes-map.ts` 한 곳이다★(2026-09-09) — CONSOLE_PATHS(콘솔 화면 폴더와 1:1) ·
ADMIN_ONLY · ADMIN_READABLE · HANBAEK_ONLY · WRITER_ONLY. `test/conventions/admin-pages.test.ts` 가 폴더 트리와
이 목록을 맞춰 보고, `console-paths.test.ts` 가 화면 폴더마다 CONSOLE_PATHS 항목이 있는지 본다 — 감사 L21
(/finance·/scan 이 빠져 포털 404 와 로그인 문을 둘 다 우회)이 그렇게 잡히는 부류다. 둘을 이날 목록에 넣었다.

### 계정 구분 다섯 — 눈과 손이 따로다

| 구분 | 보는 것 | 바꾸는 것 |
|---|---|---|
| `admin` 한백 관리자 | 전 현장 · 원가·마진·기성 | 전부 |
| `viewer` 열람 전용 | 전 현장 · 원가·마진·기성 · 협력사 정보 (계정설정만 빼고 관리자와 같다) | **없음** |
| `salesCons` 턴키업체 | 자기 소속 현장 · 영업비+시공비 | 접수·서류·공정(자기 현장) |
| `cons` 시공사 | 자기 소속 현장 · 시공비 | 접수·서류·공정(자기 현장) |
| `sales` 영업사 | 자기 소속 현장 · 영업비 | 접수·서류(자기 현장) |

`role === 'admin'` 을 그대로 쓰지 않는다 — 그 한 줄이 두 뜻으로 쓰이고 있었다.
`lib/roles.ts` 의 둘 중 그 자리에 맞는 것을 쓴다:

- **`isHanbaek(role)`** — 눈. 전 현장·원가를 보는가 (관리자 · 열람 전용).
  `accessWhere` · `listSettlements` · 수주 현황 업체별 쪼개기 · 지급 내역의 전 업체 묶음.
- **`canWrite(role)`** — 손. 무엇이든 바꾸는가 (열람 전용만 false).
- **`role === 'admin'`** — 한백만 하는 **쓰기**. 검수·계약 확인·단가 지정·지급 확정.

★서류를 손질하는 도구 셋(`/reissue` · `/split` · `/scan`)은 열람 전용도 쓴다★
(한백 지시 2026-08-31). 셋 다 우리 DB 에 아무것도 안 쓴다 — 재발행은 포털 양식으로 보내는
링크 모음이고, 분할·스캔은 파일을 갈라 받아 가는 자리다. 「내는 자리」라는 이유로 막고
있었는데 그것은 자리의 성격이지 권한이 아니었다: 재무가 받은 서류를 갈라 보고 사진을
스캔본으로 고칠 일이 있다. 판독 비용의 문(`/api/pdf-sort`)도 한백의 눈이면 연다.
접수(`/projects/new`)·계약서 작성(`/contracts`)·협력사 정보(`/settings`)는 그대로 막는다 —
거기까지 열면 「열람 전용」이 아니게 된다. 재무가 실제로 그 일을 해야 하면 계정 구분을 올린다.

열람 전용의 쓰기는 `lib/api/write-route.ts` 한 곳이 전부 막는다(403). 껍데기는 본문을 JSON 으로
읽으므로 **multipart 라우트 다섯은 그 껍데기를 못 쓴다** — 서류 올리기(현장 서류 2 · 협력사
정보 1) · 접수 파일 2. 이 다섯은 `canWrite(session.role)` 을 손으로 부르고 껍데기와 같은 403
문구를 쓴다. ★손으로 옮겨 적는 자리라 빠뜨린 적이 있다★ — 협력사 정보 올리기가 그랬고
(2026-08-25 에 메웠다), 그동안 이 문서는 있지도 않은 `requireWriter()` 를 부른다고 적고
있었다. 새 multipart 쓰기 라우트를 만들면 이 검사를 같이 적는다. ★시험이 잡는다★(2026-09-09,
`test/conventions/routes.test.ts` — POST·PUT·PATCH·DELETE 를 내보내는 라우트에 껍데기·requireAdmin·canWrite·
isHanbaek 중 하나가 없으면 빨갛다. 문이 없어도 되는 것은 그 파일의 UNGATED_BY_DESIGN 에 이유와 함께 적는다).
화면에서 단추를 감추는 것은 그 다음 일이다 — 못 하는 일은 눌리지 않게(화면 규칙 3번).

열람 전용은 **소속이 없다**(`org = null`). 소속 없는 협력사 계정은 볼 현장이 하나도 없지만
열람 전용은 반대로 전부 본다 — 판정 순서가 `isHanbaek` 먼저라서 그렇다.

### 지급조건은 확정하면 잠긴다 (2026-08-28)

단가 케이스와 정산 규칙이 계획·잔액·기성·마진을 전부 정한다. 중간에 갈아 끼우면 이미 나간
지급과 앞으로 받을 기성이 같이 뒤틀린다 — 그래서 현장마다 「지급조건 확정」이 있고
(`projects.payout_terms_confirmed_at`), 확정되면 저장소가 단가·정산 규칙 변경을 거절한다.

★지급이 나가면 자동으로 잠긴다★ (한백 지시) — 돈이 움직인 뒤의 변경이 가장 위험하다.
`runPayoutBatch`·`addPayoutEntry`(지급 유형만)가 확정일을 찍는다. 조정(자재비·차감)은 계획을
바꾸는 것이라 잠그지 않는다. 이관한 현장도 소급해서 잠갔다(migrations/0035, 확정일 = 첫 지급일).

덜 된 조건은 굳히지 않는다 — 단가 미지정 라인이 있거나 정산 규칙이 없으면 확정을 거절한다.
해제는 관리자만(화면 규칙 7) — 확정 뒤에 진짜 오류가 드러나는 일이 있다. 해제 단추에는
이미 나간 지급 건수를 적는다.

## 화면 규칙

화면을 만들 때 이 열세 개를 지킨다. 「좋은 UX 를 하자」는 말로는 아무것도 안 바뀌므로,
전부 어겼는지 눌러보고 알 수 있는 문장으로 적는다.

**모양을 고르는 곳은 두 군데다.** 규칙을 문장으로만 두면 갈린다 — 한 번 세어 보니
배지 18 모양, 주 단추 10 모양, 입력칸 8 모양, 띠 7 모양, 실패 문구 4 모양이었다.

| | |
|---|---|
| `components/ui.tsx` | 부품의 정본. 단추·배지·꼬리표·띠·실패 문구·빈 값·입력칸 |
| `/design` (`app/(console)/design/page.tsx`) | 그 부품을 실제로 그려 보는 화면. 색·활자·모서리도 여기 |
| `components/project/construction/shell.tsx` | ★시공 줄의 자리 정본★ — 이름 열 폭·줄 여백·틈 (아래 설명) |

시공 탭의 줄은 자리 정본을 따로 갖는다(2026-09-04) — `ROW`·`ROW_STACK`·`RowLabel`·
`DATE_CELL`. 일곱 줄 부품(NeedRow·CheckRow·AdvanceRow·ModelRow·CountsRow·DateRow·DocRow)이
이름 폭과 여백을 자리마다 손으로 적어 오와열이 갈려 있었다(이름 폭 w-32 다섯·w-52 하나,
여백 py-2 다섯·py-2.5 하나, 날짜 값 폭 132px·150px — 한백 지적 「오와열이 전혀 맞지
않는다」). ui.tsx 에 두지 않은 이유는 이 폭이 그 화면의 서류 이름 길이에 묶인 값이라서다 —
★새 시공 줄을 만들면 여기서 가져다 쓰고, 폭을 자리에서 적지 않는다.★

`/design` 은 클래스를 베껴 적지 않고 **`components/ui.tsx` 를 불러다 그린다** — 그래서
코드와 어긋날 수 없다. 없는 모양이 필요하면 자리에서 클래스를 적지 말고 부품에 추가한다.

**1. 상자는 한 겹이다.**
테두리 안에 테두리를 넣지 않는다. 겹칠수록 안쪽 것이 무엇에 속하는지 오히려 흐려진다 —
「진행현황 및 메모」가 카드 안 회색 상자 안 흰 상자 안 입력칸으로 네 겹이었다.
층을 나눌 때는 **약한 것부터** 쓴다: 여백 → 얇은 선(`border-t border-slate-100`) → 배경색 → 테두리.
테두리는 마지막 수단이고, 그 한 겹은 보통 입력칸·카드 자기 것이다.

**2. 설명 문구를 넣지 않는다.**
「현장마다 기설치를 확인합니다」, 「없음도 확인한 결과입니다」, 「단계는 서류·공정에서 유도됩니다」
같은 안내문은 다 걷어냈다. 규칙은 문장이 아니라 동작으로 보이게 만든다 —
못 하는 것은 눌리지 않게, 조사 안 된 것은 배지로. 이유는 코드 주석에 적는다.

**3. 못 하는 이유를 버튼 이름에 적는다.**
버튼을 흐리게만 두면 왜 안 되는지 알 수 없다. 「단가 미지정 — 계약 확인 불가」처럼
막는 것을 그 자리에 적는다. 따로 안내문을 두지 않는다(2번과 어긋나지 않는다 — 이건 상태다).

**4. 평소엔 글자, 고칠 때만 입력칸.**
늘 열린 입력칸은 옆을 지나가는 클릭에 값이 바뀐다. 확정된 값은 글자로 굳히고
「수정」을 눌러야 열린다(`EditableFact`). 예외는 표의 칸 — 여러 건을 쭉 넣는 자리다.
곁다리 동작(수정·취소)은 밑줄 글자가 아니라 고스트 칩이다(`kind="quiet"`) — 밑줄은 링크로 읽힌다.

**5. 같은 값을 한 화면에 두 번 두지 않는다.**
두 곳에 있으면 갈린다. 환경부 대기번호를 머리말로 올릴 때 「현장 정보」에서 뺐고,
기설치는 「기설치 조사」 구역이 정본이라 서류 목록에서 뺐다.

**6. 빈 값도 자리를 지킨다.**
「미지정」·「—」로 보인다. 빠뜨린 것과 원래 없는 것을 구별해야 하고,
비어 있는 것이 보이는 것도 정보다. 다만 「아직 없습니다」는 적지 않는다 — 「0건」이 그 말이다.

**7. 되돌릴 길을 같이 만든다.**
한 번 정하면 못 고치는 값을 만들지 않는다. 계정 구분·영업사 이름·계약 확인·대기번호가
전부 이것 때문에 DB 를 직접 만져야 했다. 넣는 자리를 만들면 고치는 자리도 만든다.

**8. 자주 누르는 것과 되돌리기 어려운 것을 붙여 두지 않는다.**
반려·삭제는 미리보기·다운로드와 같은 줄의 반대쪽 끝(`ml-auto`)이다.

**9. 누른 뒤 무엇이 되었는지 그 자리에 보인다.**
도는 중에는 단추 이름이 「저장 중…」으로 바뀌고(`<Btn busy>`), 실패 문구는 **누른 단추 옆**에
뜬다(`<Err>`). 화면 위쪽 한 곳에 모아 두면 무엇을 누르다 틀렸는지 알 수 없다.
「저장됨」은 잠깐 뜨고 사라지게 만들지 않는다 — 못 보고 지나친다.

**10. 빈 값은 네 가지고 서로 다른 말이다.**
「미지정」(넣어야 하는데 안 넣음 · 노랑) · 「—」(아직 올 때가 아님) ·
「해당없음」(규칙상 없음 · 고치는 자리를 주지 않는다) · 「0건」(세었고 없음).
하나로 뭉치면 빠뜨린 것과 원래 없는 것이 같아 보인다. 자체투자의 대기번호·기설치가 그 자리다.

**11. 동글면 상태, 각지면 누르는 것.**
`rounded-full` 은 못 누르는 상태 배지(한 건에 하나 — 계약/시공/멈춤),
`rounded-tag` 는 세어진 꼬리표(반려 2 · 단가 미지정), `rounded-ctl` 은 누르는 것과 입력칸이다.
모양이 눌리는지를 먼저 말한다.

**12. 빨강 배경은 되돌릴 수 없는 것을 확정할 때만.**
「반려」를 여는 단추와 「반려 확정」이 둘 다 빨갛던 적이 있다. 다 빨가면 어느 것이
되돌릴 수 없는 것인지 알 수 없다. 여는 자리는 글자만(`kind="undo"`), 확정만 빨강(`kind="stop"`).

**13. 표의 칸은 가운데. 금액 열만 오른쪽(`money`), 현장 열만 왼쪽(`left`).** (한백 결정 2026-09-02)
표의 칸은 `<Th>`·`<Td>` 를 쓴다 — 정렬은 부품이 정하고 자리에서 적지 않는다.
금액은 일의 자리가 한 줄에 서야 크기가 읽혀서 오른쪽이고(가운데는 tabular-nums 로도
끝이 안 선다), 현장은 줄을 찾는 열쇠라 왼쪽이다. ★예외는 이 둘로 끝★ — 건수·대수·
날짜·상태는 가운데다(`num` 은 자릿수 글꼴만 준다).

★셋째가 하나 늘었다 — 여러 줄 글·불릿은 왼쪽이다★ (한백 2026-09-06 「표 안에 있는 텍스트랑
bulletpoint는 다 왼쪽정렬로」). 단가표의 조건 행(설치조건·기타·병행주차…)이 그 자리다:
문단을 가운데 두면 줄마다 시작점이 달라 조건이 몇 개인지 세어지지 않는다 — 불릿이 특히
그렇다. 가르는 자리는 「값이냐 글이냐」다: 한 낱말 값(수전·연수·상태)은 그대로 가운데고,
줄이 쌓이는 칸(`whitespace-pre-line`·여러 줄이 들어오는 열)만 왼쪽이다. 넷째는 만들지 않는다.
좌우로 가르던 규칙(숫자 오른쪽·글 왼쪽)은 열마다 다른 것 자체가 「통일 안 됨」으로
읽혀 걷었다. 금액·건수에는 `num`(자릿수 고정 글꼴)을 준다 — 폭이 같아 가운데서도
자리가 흐트러지지 않는다. **머리는 줄바꿈하지 않는다**(「총 지급액」이 두 줄로 접히던
자리). 좁으면 접지 말고 `Frame` 으로 가로로 민다.

- 현장 상세는 **계약 · 시공 · 협력사 지급** 탭에 한백만 보는 **기성** 탭이 붙는다.
  계약이 끝나기 전에는 시공 탭이 잠긴다.
- **검수는 예외를 걸러내는 방식이다.** 제출된 서류는 기본이 통과이고, 문제 있는 것만 반려한다. 승인 버튼을 만들지 않는다.
  **안 낸 서류는 칸마다 반려할 수 없다** — 반려는 올라온 파일에 대한 판정이라 저장소가
  미제출 검수를 거절한다. 그 자리는 계약 탭의 「누락 서류 N건 보완요청」이다
  (`askMissingDocs`, 한백 지시 2026-08-25): 파일이 없는 필수 칸을 한 번에 반려로 세워
  계약보완으로 내린다. 겨냥은 **파일 없는 칸**뿐이고(올라온 서류의 문제는 그 칸의 반려가
  다룬다), 접수 전 계약과 노션 이관 현장은 대상이 아니다. 되돌리는 자리는 반려 띠의
  「보완요청 취소」다.
- **계약 서류에 협력사가 손댈 수 있는 구간은 두 단계로 닫힌다** (`lib/process.ts`
  `canChangeContractDocs` 한 곳이 판정하고 화면·저장소가 같이 본다).
  ① **운영사 계약서 제출부터 잠긴다**(`CONTRACT_DOCS_LOCK_AT`, 한백 지시 2026-08-29) — 낸 서류가
  정본이다. 예외 둘: 반려·보완요청으로 **확인이 풀린 동안**(고쳐 오라고 했으면 고칠 수 있어야 한다,
  2026-08-31)과 **빈 칸**(낸 것이 없으니 정본을 바꾸는 게 아니다, 2026-09-03).
  ② **착공부터는 닫힌다**(`PARTNER_DOCS_CLOSED_AT`, ★한백 지시 2026-09-05★) — 공사가 도는
  현장의 서류는 **한백이 받아서 올린다**. 빈 칸 예외도 여기서 같이 닫힌다. 유일한 열림은
  한백이 **「확인 취소」로 계약을 다시 연 경우**다 — 그러면 현장이 계약 보드로 내려가 협력사
  차례가 되므로 협력사가 고친다(그건 「공사 중」이 아니다). 착공 뒤 한백이 반려해도 담당을
  협력사에게 넘기지 않는다(기록용 — 할 일·보드는 저장된 담당이 아니라 칸에서 차례를 유도한다).
  협력사 화면의 반려 띠는 ★실제로 올릴 수 있는가★로 말을 가른다 — 올릴 수 있으면 「다시
  올려주세요」, 없으면 「고친 서류는 한백에 보내주세요」. 기설치 조사 구역의 올리기 단추도
  같은 판정을 따른다.
  ★반려된 칸을 착공 뒤에도 협력사가 다시 올리게 하는 예외를 하루 두었다가 걷었다★(2026-09-04→05):
  첫 장을 넣는 순간 예외가 사라져 두 장짜리 스캔이 반쪽으로 잠겼다. 예외를 정교하게 만드는 대신
  문을 닫는 쪽을 골랐다. 착공 전 빈 칸 예외에는 그 결함이 남아 있다(한 칸에 1장만 들어간다).
- **서류 한 칸에 파일 여러 장이 붙는다** (`documents.files`·`process_documents.files`,
  migrations/0021, 한백 지시 2026-08-25). 회의록이 두 장으로 스캔되거나 사진대지가 동별로
  갈려 오는 일이 흔하다. 올리면 **쌓인다** — 갈아치우지 않고, 앞 파일을 지우지도 않는다
  (`lib/attach-doc.ts`). `files` 가 파일 목록의 정본이고 `filename`·`blob_url` 은 첫 파일의
  사본이다(손으로 SQL 을 볼 때를 위해 같이 맞춘다). 잘못 올린 장은 **올린 쪽이 뺀다**
  (`deleteDocumentFile` — 그 현장의 협력사·한백). 마지막 장을 빼면 그 칸은 미제출로
  돌아간다. 칸을 통째로 비우는 것(`deleteDocument`)은 여전히 한백만 한다.
  ★필수 서류가 찼는지는 파일로 센다★(`lib/stage.ts` `docsFilled`) — 상태로 세면 파일 없이
  반려로 세운 칸(누락 서류 보완요청)이 「찼다」로 잡힌다.
- 단계(`stage`)는 저장값이 아니라 서류·단가에서 유도된다(`lib/stage.ts`). 버튼으로 단계를 바꾸지 않는다.
  **단추가 저장하는 것은 「사람이 한 일」이고, 칸은 그것에서 유도된다** — 계약의 세 칸이
  그 예다: 협력사가 「계약서 접수하기」(`contract_submitted_at`) → 계약검토, 한백이
  「계약 확인 완료」(`contract_confirmed_at`) → 계약완료, 서류를 반려하면 → 계약보완.
  반려는 보완요청 이력을 남긴다(`contract_fix_asked_at`, 지우지 않는다) — 보완이 풀린
  계약은 접수 선언이 없어도 **계약검토**에 선다(계약접수는 처음 모으는 자리다).
  협력사가 내는 행위의 이름만 「계약 재검토 요청」으로 갈린다(한백 지시 2026-08-25).
  칸 이름을 저장하는 자리는 없다(`lib/board.ts` 가 정본).
- **원가·마진·기성은 협력사에게 보이지 않는다.** 화면에서 가리는 것으로는 부족하다 — 서버가 렌더한 데이터가 브라우저에 통째로 실리므로 저장소 계층에서 지운다(`redactForViewer`).

## 코드 규칙

- 저장소는 `ProjectRepository` 인터페이스 뒤에 있다. 화면은 구현을 모른다.
- **DB 쿼리를 `Promise.all` 로 그냥 묶지 않는다** — `lib/data/db-slot.ts` 의 `allSlots` 를 쓴다.
  풀보다 많이 던지면 큐가 풀리지 않고 요청이 통째로 죽는다(실사고: `/payouts` 300초
  타임아웃, 2026-08-21). 슬롯을 쥔 채 슬롯을 기다리면 서로 막으므로 **잎 쿼리만** 감싼다.
  ★lint 가 잡는다★(2026-09-09, `.eslintrc.json` no-restricted-syntax — `Promise.all([...])` 안에 쿼리 체인
  `.from(` 이 직접 들어가면 오류). 트랜잭션 안에서는 커넥션이 하나라 나란히 던질 이유가 없다 — 차례로 읽는다.
- 쓰기는 `Actor` 를 받고 `audit_log` 를 남긴다.
- 한백 전용 쓰기는 라우트에서 `requireAdmin()`, 저장소에서 `assertAdmin()` 으로 두 번 막는다.
- ★**돈·권한·게이트를 고치면 `npm test`**★ (2026-08-28 도입) — `test/` 에 순수 함수 그물이
  있다(301개, 0.5초). 대상은 `lib/process`(게이트·전이·권한) · `lib/roles`(가시성) ·
  `lib/settlement`(기성·지급 회차) · `lib/payout-board`(배치) · `lib/pricing-match`(단가 축) ·
  `lib/doc-rules`(서류) · `lib/date`(한국 달력). 화면 테스트는 넣지 않는다 — 느려지면 안 돌린다.
  2026-09-08 부터 CI 가 모든 브랜치 푸시에서 tsc·test·lint·마이그레이션 번호를 검사하고 main 은 통과한 커밋만 받는다
  (하네스 2번). 「급한 고침이 막힌다」는 2분짜리 검사에는 맞지 않았다. 고친 사람이 먼저 돌리는 습관은 그대로다 —
  CI 는 잊었을 때의 그물이다.
- ★**라우트·저장소의 문(권한·검증·동시성)을 고치면 `npm run test:db`**★ (2026-09-08 도입, 하네스 3번) —
  `test/db/` 의 경계 시험이 ★개발 DB★에 대고 라우트 핸들러를 함수로 부른다(Next 없이 · 세션은 next/headers
  흉내에 쿠키를 넣는다 · 도구는 `test/db/kit.ts` 의 signIn·withProject·call). 감사 높음 9건이 전부 이 층이었다.
  지금 있는 것: 쓰기 껍데기의 응답 계약(401·403·400·422·200) · 남의 현장·한백 전용 칸·오지 않은 구간 거부 ·
  협력사 응답의 금지 키(check:leak 과 같은 목록) · 같은 칸 동시 업로드. ★남은 감사 항목을 고칠 때는 여기에
  재현 시험을 먼저 쓴다★ — M10 이 그렇게 갔다(시험이 첫 실행에서 빨갔고, 자문 잠금을 넣자 초록).
  setup 이 DATABASE_URL 의 프로젝트 ref 를 보고 개발 DB 가 아니면 멈춘다. 시험 현장은 이름에 실행 표지를
  박고 끝나면 지운다. CI 의 `db-check` 는 `DEV_DATABASE_URL`·`AUTH_SECRET` 비밀이 있을 때만 돈다(없으면 건너뜀).
- **예상 밖 오류는 500 과 `[api]` 로그다** (2026-09-09, 하네스 4번) — 쓰기 껍데기(`lib/api/write-route.ts`)가
  `isUnexpectedError`(`lib/api/errors.ts`, 순수 모듈)로 가른다: 저장소의 규칙 위반(맨 Error·한글 메시지)은 422,
  값 잘못(BadRequest)은 400, 그 밖(DB 드라이버·드리즐·TypeError 류·Error 아닌 것)은 `console.error('[api]', …)`
  뒤 500 과 일반 문구. 그전에는 전부 422 로 삼켜져 협력사 화면에 드라이버 메시지가 나가고 로그에는 아무 줄도
  없었다. 서버에서 「사람이 봐야 할」 일이 생기면 `[db]`·`[auth]`·`[api]` 처럼 대괄호 태그로 console.error 를
  남긴다 — 감시(아래 「운영 감시」)가 그 태그를 본다. ★lint 가 잡는다★(2026-09-09, `lib/**`·`app/api/**`·
  `middleware.ts` 의 console.error 첫 인자가 `[태그]` 로 시작하지 않으면 오류).
- `next build` 를 `next dev` 가 돌고 있는 중에 실행하지 않는다. `.next` 가 깨진다.
- 비밀번호·접속 문자열을 대화에 남기지 않는다. `.env.local` 에 직접 넣는다.
- **함수 지역은 `icn1`(서울) 이다** — `vercel.json` 의 `regions`. DB(Supabase `ap-northeast-2`)와
  같은 도시여야 한다. 기본값 `iad1`(버지니아) 에서는 질의마다 태평양을 건너 180~250ms 씩
  붙는다. 콘솔은 화면 하나에 여러 번 왕복하므로 그대로 느려진다. JSON 에 주석을 못 달아
  여기 적는다.

## 협업 방식

- ★**세션은 워크트리에서 일한다 — main 은 머지로만 바뀐다**★ (2026-09-08 도입, 하네스 1번). 이 저장소는
  Claude 세션 여럿이 동시에 쓴다. 한 작업 트리를 나눠 쓰면 인덱스도 파일도 섞인다 — 실사고 셋: 남의
  스테이징을 커밋에 삼킴(2026-08-21) · 옆 세션의 미완성 코드가 내 커밋에 실려 프로덕션 빌드 두 번 깨짐
  (2026-08-29, 인터페이스 없는 `listTodoSources`) · 세션 둘이 마이그레이션 0064 를 같이 집어 프로덕션 빌드
  깨짐(2026-09-07). 워크트리는 세션마다 작업 트리·인덱스·브랜치를 따로 준다. 충돌은 사라지는 게 아니라
  rebase 때 내 워크트리에서 **눈에 보이게** 난다 — 그게 낫다.
  - **시작**: `cd ~/Documents/hanbaek-ev && claude -w <이름>` — `.claude/worktrees/<이름>/` 에 브랜치
    `worktree-<이름>` 이 `origin/main` 에서 갈라진다. 이름을 빼면 무작위 이름. `claude` 로 그냥 시작했으면
    ★첫 일로 `EnterWorktree` 를 부른다★. 본 저장소(main 이 체크아웃된 `~/Documents/hanbaek-ev` 자체)에서는
    일하지 않는다. 홈 디렉터리에서 시작하면 저장소 설정(훅)을 안 읽으니 저장소에서 시작한다.
  - **준비는 자동이다**: `.claude/settings.json` 의 `worktree.symlinkDirectories` 가 `node_modules` 를
    링크하고, `.worktreeinclude` 가 `.env.local`·`.vercel/project.json` 을 복사한다. 그래서 워크트리에서
    바로 `npm test`·`npx tsc --noEmit`·`npx vercel` 이 된다. `git worktree add` 로 손으로 만들었으면
    `bash scripts/worktree-setup.sh` 가 같은 일을 한다. `.env.prod-db` 는 복사하지 않는다 — 프로덕션
    DB 스크립트는 본 저장소에서 돌린다.
  - **커밋**은 브랜치에 자유롭게. 경로 지정 규칙(옛 `commit -- <파일>`)은 필요 없다 — 인덱스가 내 것이다.
    커밋·브랜치 푸시는 세션이 알아서 한다.
  - **프리뷰**: `git push -u origin <브랜치>` → 약 1분 뒤
    `https://hanbaek-form-git-<브랜치>-kjw5757-1767s-projects.vercel.app` (브랜치 이름의 `/` 는 `-`).
    ★사용자는 프리뷰를 보고 판단한다★ — 프로덕션이 아니라. Vercel 로그인이 필요하다(curl 은 401, 상태는
    `npx vercel ls` 로). DB 는 개발 DB(시드 5건 · `admin`/`dev1234!`). ★Blob 은 프로덕션 스토어를 같이
    쓴다★ — 파일 올리기 시험은 프로덕션 자료실에 남는다. 실데이터가 걸린 확인만 머지 뒤 프로덕션에서.
  - **머지 = 배포**: 사용자가 「올려」 하면 워크트리에서
    `git fetch origin && git rebase origin/main && git push --force-with-lease origin <브랜치>` 로 브랜치를 최신
    main 위에 세운다. 그러면 CI(`.github/workflows/ci.yml` 의 `check` — tsc·test·lint·마이그레이션 번호, 약 2분)가
    그 SHA 를 검사한다. ★초록이 된 뒤★ `git push origin HEAD:main`. main 은 보호 브랜치다 — 검사를 통과한
    커밋만 받고(관리자도 못 우회) 강제 푸시·삭제·머지 커밋을 거부한다. 통과 전에 밀면 원격이
    「Required status check "check" is expected」로 거부한다. 상태는 `https://github.com/tonykim0/hanbaek-ev/actions`
    또는 `https://api.github.com/repos/tonykim0/hanbaek-ev/commits/<sha>/check-runs` 로 본다(공개 저장소라 인증 없이).
    fast-forward 만 된다. 본 저장소는 건드리지 않는다 — 워크트리 세션이 본 저장소로 보내는 git 은 Claude Code 가
    막는다. 본 저장소의 로컬 `main` 은 뒤처져도 상관없다(새 워크트리는 `origin/main` 에서 갈라진다). 급한 고침도
    같은 길이다 — 건너뛸 수 있는 것은 프리뷰 확인뿐이고 검사는 아니다.
  - **정리**: 머지가 끝난 워크트리는 지운다 — 세션을 끝낼 때 Claude Code 가 묻는다(remove). 원격 브랜치는
    `git push origin --delete <브랜치>`.
  - **훅** `.claude/hooks/guard-git.sh`(PreToolUse) 가 기계적으로 지킨다: 본 저장소·main 에서의
    commit·rebase·reset·cherry-pick·revert 거부 · main 으로 강제 푸시·삭제 거부 · main 으로 푸시 전
    마이그레이션 번호 겹침 검사(겹치면 거부 — 올라가면 프로덕션 빌드가 깨지니까).
  - 마이그레이션 번호는 워크트리에서도 「디렉터리 최대 번호 + 1」. 다른 브랜치가 같은 번호를 먹었을 수
    있으니 rebase 뒤 다시 본다 — 훅과 러너가 잡아 주지만, 잡히면 그때 고치는 것이 일이다.
- 커밋 메시지는 한글 — 제목이 결론, 본문이 왜. 기능 단위로 잘게.
- 규칙·우선순위가 바뀌면 이 파일을 같이 고친다 — md 가 낡으면 다음 세션이 옛 규칙으로 일한다.
  「다음 할 일」은 끝난 것을 지우고 새것을 위에 놓는다.
